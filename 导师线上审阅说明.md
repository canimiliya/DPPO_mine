# DPPO 项目导师线上审阅说明

## 1. 项目用途与当前结论

本仓库用于导师在线审阅论文 **Diffusion Policy Policy Optimization (DPPO), ICLR 2025** 的 Gym/MuJoCo 复现工程，包括代码来源、运行环境、实验配置、兼容性处理、已执行日志和关键 checkpoint。

截至 2026-09-11，项目状态为：

- Windows/WSL2、CUDA、MuJoCo、D4RL、Gym 和 DPPO 环境验证：**通过**。
- Hopper 的 DPPO、Gaussian-MLP 短 smoke test：**通过**。
- Hopper Figure 4 DPPO 官方规模的 5-iteration 性能基准：**通过**。
- Figure 4 三环境、七方法的 seed=42 完整训练：**尚未完成**。
- Figure 18 三环境、两方法的 seed=42 完整 scratch 训练：**尚未完成**。
- Figure 4、Figure 18 最终静态复现图：**尚未生成**。

因此，本仓库当前可以证明复现环境和主要训练链路已经跑通，但不能作为论文 Figure 4 或 Figure 18 已完整复现的证据。

## 2. GitHub 仓库信息

- 仓库：https://github.com/canimiliya/DPPO_mine
- 默认分支：`main`
- 当前可见性：**Private**。导师需要被授予 GitHub 仓库访问权限后才能在线查看。
- 官方 DPPO 源码以 Git submodule 保存于 `source/dppo_v0.6`。
- 官方版本：`v0.6`。
- 官方源码 commit：`dc8e0c9edce7ac2b2ff112abe460e1c21b0b3bdc`。

完整克隆时应使用：

```bash
git clone --recurse-submodules https://github.com/canimiliya/DPPO_mine.git
```

如果已经普通克隆，可在仓库根目录执行：

```bash
git submodule update --init --recursive
```

## 3. 本地项目位置

Windows 项目根目录：

```text
D:\Desktop\my_project\paper_reproduction\DPPO
```

WSL2 对应目录：

```text
/mnt/d/Desktop/my_project/paper_reproduction/DPPO
```

所有本次复现产生的代码、数据、环境、checkpoint、日志、报告和绘图脚本均集中在该项目根目录。除 Ubuntu 系统级依赖外，没有把实验资产分散到其他研究项目中。

## 4. 本地目录结构、用途与大小

下表中的大小是 2026-09-11 发布审计时的本地值。更细的逐文件路径、字节数和 SHA256 见 [`reports/RESOURCE_MANIFEST.md`](reports/RESOURCE_MANIFEST.md) 和 [`reports/RESOURCE_MANIFEST.csv`](reports/RESOURCE_MANIFEST.csv)。

| 本地目录或文件 | 大小 | 用途 | 是否上传 GitHub |
|---|---:|---|---|
| `.runtime/` | 6.71 GiB | WSL Python 虚拟环境、CUDA Python 库、MuJoCo 2.1.0、gdown 工具和 mjrl 源码 | 否 |
| `.runtime/venv/` | 6.70 GiB | 本项目独立的 WSL Python 环境 | 否 |
| `.runtime/mujoco210/` | 8.38 MiB | legacy Gym/D4RL 使用的 MuJoCo 2.1.0 Linux runtime | 否 |
| `.runtime/gdown_tools/` | 6.01 MiB | Windows 侧 Google Drive 下载工具 | 否 |
| `.runtime/mjrl_source/` | 412.36 KiB | D4RL 所需第三方 mjrl 依赖源码 | 否 |
| `.cache/` | 3.79 GiB | pip/Torch 下载与编译缓存 | 否 |
| `.tmp/` | 4.18 MiB | MuJoCo 下载压缩包等审计保留文件 | 否 |
| `source/` | 约 1.67 MiB | 官方 DPPO v0.6 源码；GitHub 中记录为 submodule | 是，submodule |
| `data/` | 233.86 MiB | 三个 D4RL medium 数据集和 normalization 文件 | 否 |
| `checkpoints/official/` | 12.95 MiB | 三个官方 diffusion policy 预训练 checkpoint | 否 |
| `logs/` | 28.25 MiB | smoke test、Hydra 配置、benchmark 日志和本机 checkpoint | 部分上传 |
| `scripts/` | 约 34 KiB | 训练、绘图和资源清单脚本 | 是 |
| `reports/` | 约 228 KiB | 环境、版本、兼容性、实验定义、运行记录和资源清单 | 是 |
| `results/` | 0 B | 预留给正式实验 CSV/NPZ | 空目录，Git 不跟踪 |
| `plots/` | 0 B | 预留给正式复现图和 side-by-side 图片 | 空目录，Git 不跟踪 |
| `DPPO.pdf` | 17.68 MiB | 用户提供的论文原文 | 否 |
| `图4.png` | 426,226 B | 用户提供的 Figure 4 对照截图 | 否 |
| `图18.png` | 129,218 B | 用户提供的 Figure 18 对照截图 | 否 |

## 5. 本地环境

| 组件 | 实际环境 |
|---|---|
| 主机 | Windows + WSL2 Ubuntu 24.04 |
| Python | 3.12.3 |
| Python 环境 | `D:\Desktop\my_project\paper_reproduction\DPPO\.runtime\venv` |
| PyTorch | `2.7.1+cu128` |
| CUDA runtime | 12.8 |
| GPU | NVIDIA GeForce RTX 5060 Ti，compute capability `(12, 0)` |
| Gym | 0.22.0 |
| D4RL | 1.1 |
| mujoco-py | 2.1.2.14 |
| MuJoCo | 2.1.0 |
| NumPy | 1.26.4 |
| Hydra / OmegaConf | 1.3.2 / 2.3.0 |

论文年代的 PyTorch 2.4.0 没有机械照搬。本机采用 PyTorch 2.7.1+cu128 以支持 RTX 5060 Ti/Blackwell；该变化仅属于运行基础设施兼容，不改变 DPPO 算法、环境动力学、reward、数据集、网络结构、action chunk、denoising steps 或 PPO 超参数。详细说明见 [`reports/COMPATIBILITY_PATCHES.md`](reports/COMPATIBILITY_PATCHES.md)。

正式运行使用的关键环境变量：

```text
DPPO_DATA_DIR=/mnt/d/Desktop/my_project/paper_reproduction/DPPO/data
DPPO_LOG_DIR=/mnt/d/Desktop/my_project/paper_reproduction/DPPO/logs
MUJOCO_PY_MUJOCO_PATH=/mnt/d/Desktop/my_project/paper_reproduction/DPPO/.runtime/mujoco210
MUJOCO_GL=osmesa
LD_LIBRARY_PATH=/mnt/d/Desktop/my_project/paper_reproduction/DPPO/.runtime/mujoco210/bin:/usr/lib/x86_64-linux-gnu:/usr/lib
```

## 6. 数据与官方 checkpoint

本地 D4RL 数据：

| 环境 | 数据文件 | 大小 | 形状 |
|---|---|---:|---|
| Hopper | `data/gym/hopper-medium-v2/train.npz` | 54.71 MiB | states `(999906, 11)`；actions `(999906, 3)` |
| Walker2D | `data/gym/walker2d-medium-v2/train.npz` | 88.45 MiB | states `(999762, 17)`；actions `(999762, 6)` |
| HalfCheetah | `data/gym/halfcheetah-medium-v2/train.npz` | 90.68 MiB | states `(999000, 17)`；actions `(999000, 6)` |

每个环境目录还包含 `normalization.npz`。这些数据不上传 GitHub，但逐文件 hash 已在资源清单中保存。

官方预训练 checkpoint 位于：

```text
D:\Desktop\my_project\paper_reproduction\DPPO\checkpoints\official
```

包括 Hopper、Walker2D、HalfCheetah 三个 `state_3000.pt`，总计 12.95 MiB。它们用于 Figure 4 各方法共享同一 pretrained diffusion policy；Figure 18 scratch 实验禁止使用这些 checkpoint。

## 7. GitHub 已上传的关键运行证据

### Hopper Figure 4 DPPO benchmark

- 运行配置：40 environments × 500 steps，seed=42，`n_train_itr=5`。
- 日志目录：[`logs/gym-finetune/hopper-medium-v2_ppo_diffusion_mlp_ta4_td20_tdf10/2026-09-08_22-27-40_42`](logs/gym-finetune/hopper-medium-v2_ppo_diffusion_mlp_ta4_td20_tdf10/2026-09-08_22-27-40_42)
- 关键 checkpoint：[`state_4.pt`](logs/gym-finetune/hopper-medium-v2_ppo_diffusion_mlp_ta4_td20_tdf10/2026-09-08_22-27-40_42/checkpoint/state_4.pt)
- checkpoint 大小：4,977,987 bytes。
- SHA256：`C8D5AB7E0B3DBE80578FF1984BFCBDE7C6E277DCE0D8D066396D4A2D9C3FF26B`。
- 测得稳定速度：约 91–93 秒/iteration。
- 按 1000 iterations 线性估算：约 25.4 小时/单次 Figure 4 DPPO 运行。

### Hopper Figure 18 Gaussian-MLP smoke

- 运行规模：2 environments、4 steps、2 iterations，seed=42。
- 日志目录：[`logs/gym-finetune/hopper-medium-v2_nopre_ppo_gaussian_mlp_ta1/2026-09-08_22-22-30_42`](logs/gym-finetune/hopper-medium-v2_nopre_ppo_gaussian_mlp_ta1/2026-09-08_22-22-30_42)
- 关键 checkpoint：[`state_1.pt`](logs/gym-finetune/hopper-medium-v2_nopre_ppo_gaussian_mlp_ta1/2026-09-08_22-22-30_42/checkpoint/state_1.pt)
- checkpoint 大小：4,831,875 bytes。
- SHA256：`D0C0BEDBBCE1AED382DE9960C9113B9A7EBDED404555E604D83E5CD0EE6B180A`。

短 smoke 的 rollout 只有 4 steps，没有完整 episode，因此其中的 0 evaluation reward 不能解释为算法性能，也不能用于绘制论文复现曲线。

## 8. 脚本入口

| 脚本 | 用途 | 当前是否执行完整实验 |
|---|---|---|
| [`scripts/train/run_fig4_seed42.ps1`](scripts/train/run_fig4_seed42.ps1) | Figure 4 单 seed 训练；支持环境、方法和 seed 参数 | 尚未完整执行 |
| [`scripts/train/run_fig18_seed42.ps1`](scripts/train/run_fig18_seed42.ps1) | Figure 18 DPPO/Gaussian scratch 单 seed 训练 | 尚未完整执行 |
| [`scripts/train/run_fig4_5seeds.ps1`](scripts/train/run_fig4_5seeds.ps1) | Figure 4 五 seed 批处理入口 | 已准备，未启动 |
| [`scripts/train/run_fig18_5seeds.ps1`](scripts/train/run_fig18_5seeds.ps1) | Figure 18 五 seed 批处理入口 | 已准备，未启动 |
| [`scripts/plot/make_reproduction_plots.py`](scripts/plot/make_reproduction_plots.py) | 只读取真实 `run.log` 生成 CSV 和静态曲线 | 尚无完整日志可供最终绘图 |
| [`scripts/plot/make_side_by_side.py`](scripts/plot/make_side_by_side.py) | 论文截图与本地复现图并排比较 | 尚未生成最终对照图 |
| [`scripts/setup/inventory_resources.py`](scripts/setup/inventory_resources.py) | 刷新本地资源路径、大小和 hash 清单 | 已执行 |

五 seed 脚本仅用于后续扩展，当前没有自动启动 135 个论文规模训练任务。

## 9. 建议导师在线审阅顺序

1. 阅读本文件，确认项目范围和当前完成度。
2. 阅读 [`reports/REPRO_VERSION.md`](reports/REPRO_VERSION.md)，核对官方源码版本。
3. 阅读 [`reports/EXPERIMENT_CONFIG.md`](reports/EXPERIMENT_CONFIG.md)，核对论文与官方 v0.6 实验参数。
4. 阅读 [`reports/COMPATIBILITY_PATCHES.md`](reports/COMPATIBILITY_PATCHES.md)，核对 Blackwell/WSL 兼容性是否改变科学定义。
5. 阅读 [`reports/RUN_LOG.md`](reports/RUN_LOG.md) 并查看上传的 Hydra 配置和 `run.log`。
6. 阅读 [`reports/RESULTS.md`](reports/RESULTS.md)，确认当前没有把 smoke 或 benchmark 冒充正式结果。
7. 如需核对本地未上传资源，查看 [`reports/RESOURCE_MANIFEST.md`](reports/RESOURCE_MANIFEST.md)。

## 10. 当前缺口与后续验收条件

项目要达到“第一阶段正式完成”，仍需：

- 完成 Figure 4：Hopper、Walker2D、HalfCheetah，seed=42，七种方法的真实训练；
- 完成 Figure 18：三个环境的 DPPO scratch 与 Gaussian-MLP scratch；
- 为每条曲线保存原始日志和 CSV/NPZ；
- 生成 `fig4_gym_reproduced_seed42.png` 和 `fig18_reproduced_seed42.png`；
- 生成两张论文原图与复现图的 side-by-side 对照图；
- 在 `reports/RESULTS.md` 填写最终 reward、最好 reward、收敛趋势、wall-clock 和与论文的最大偏差。

在这些条件满足之前，仓库状态应保持为“工程链路已验证，正式科学复现未完成”。
