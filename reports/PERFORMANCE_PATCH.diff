diff --git a/agent/finetune/train_ppo_diffusion_agent.py b/agent/finetune/train_ppo_diffusion_agent.py
index 998c638..75a84a4 100644
--- a/agent/finetune/train_ppo_diffusion_agent.py
+++ b/agent/finetune/train_ppo_diffusion_agent.py
@@ -11,6 +11,7 @@ import torch
 import logging
 import wandb
 import math
+from time import perf_counter
 
 log = logging.getLogger(__name__)
 from util.timer import Timer
@@ -52,6 +53,7 @@ class TrainPPODiffusionAgent(TrainPPOAgent):
         last_itr_eval = False
         done_venv = np.zeros((1, self.n_envs))
         while self.itr < self.n_train_itr:
+            iter_wall_t0 = perf_counter()
             # Prepare video paths for each envs --- only applies for the first set of episodes if allowing reset within iteration and each iteration has multiple episodes from one env
             options_venv = [{} for _ in range(self.n_envs)]
             if self.itr % self.render_freq == 0 and self.render_video:
@@ -150,6 +152,10 @@ class TrainPPODiffusionAgent(TrainPPOAgent):
                 # count steps --- not acounting for done within action chunk
                 cnt_train_step += self.n_envs * self.act_steps if not eval_mode else 0
 
+            if self.device.startswith("cuda"):
+                torch.cuda.synchronize()
+            rollout_s = perf_counter() - iter_wall_t0
+
             # Summarize episode reward --- this needs to be handled differently depending on whether the environment is reset after each iteration. Only count episodes that finish within the iteration.
             episodes_start_end = []
             for env_ind in range(self.n_envs):
@@ -193,7 +199,10 @@ class TrainPPODiffusionAgent(TrainPPOAgent):
                 log.info("[WARNING] No episode completed within the iteration!")
 
             # Update models
+            prep_s = 0.0
+            update_s = 0.0
             if not eval_mode:
+                prep_t0 = perf_counter()
                 with torch.no_grad():
                     obs_trajs["state"] = (
                         torch.from_numpy(obs_trajs["state"]).float().to(self.device)
@@ -302,7 +311,12 @@ class TrainPPODiffusionAgent(TrainPPOAgent):
                 )
                 logprobs_k = torch.tensor(logprobs_trajs, device=self.device).float()
 
+                if self.device.startswith("cuda"):
+                    torch.cuda.synchronize()
+                prep_s = perf_counter() - prep_t0
+
                 # Update policy and critic
+                update_t0 = perf_counter()
                 total_steps = self.n_steps * self.n_envs * self.model.ft_denoising_steps
                 clipfracs = []
                 for update_epoch in range(self.update_epochs):
@@ -382,6 +396,10 @@ class TrainPPODiffusionAgent(TrainPPOAgent):
                     if flag_break:
                         break
 
+                if self.device.startswith("cuda"):
+                    torch.cuda.synchronize()
+                update_s = perf_counter() - update_t0
+
                 # Explained variation of future rewards using value function
                 y_pred, y_true = values_k.cpu().numpy(), returns_k.cpu().numpy()
                 var_y = np.var(y_true)
@@ -417,10 +435,25 @@ class TrainPPODiffusionAgent(TrainPPOAgent):
                 self.save_model()
 
             # Log loss and save metrics
+            if self.device.startswith("cuda"):
+                torch.cuda.synchronize()
+            total_s = perf_counter() - iter_wall_t0
+            log.info(
+                "[PERF] itr=%d rollout_s=%.6f prep_s=%.6f update_s=%.6f total_s=%.6f",
+                self.itr,
+                rollout_s,
+                prep_s,
+                update_s,
+                total_s,
+            )
             run_results.append(
                 {
                     "itr": self.itr,
                     "step": cnt_train_step,
+                    "perf_rollout_s": rollout_s,
+                    "perf_prep_s": prep_s,
+                    "perf_update_s": update_s,
+                    "perf_total_s": total_s,
                 }
             )
             if self.save_trajs:
diff --git a/model/diffusion/diffusion_ppo.py b/model/diffusion/diffusion_ppo.py
index 1c574a3..366ad59 100644
--- a/model/diffusion/diffusion_ppo.py
+++ b/model/diffusion/diffusion_ppo.py
@@ -135,12 +135,15 @@ class PPODiffusion(VPGDiffusion):
         advantages = advantages.clamp(min=advantage_min, max=advantage_max)
 
         # denoising discount
-        discount = torch.tensor(
-            [
-                self.gamma_denoising ** (self.ft_denoising_steps - i - 1)
-                for i in denoising_inds
-            ]
-        ).to(self.device)
+        exponent = self.ft_denoising_steps - denoising_inds - 1
+        discount = torch.pow(
+            torch.tensor(
+                self.gamma_denoising,
+                device=advantages.device,
+                dtype=advantages.dtype,
+            ),
+            exponent.to(dtype=advantages.dtype),
+        )
         advantages *= discount
 
         # get ratio
